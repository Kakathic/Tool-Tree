<?xml version="1.0" encoding="utf-8"?>
<!-- actionLayout của item action_confirm_selection (nút "Xong" toolbar) - thêm gạch ngăn
     cách với icon bên trái + vùng bấm/ripple rộng hơn khung chữ mặc định của action item
     dạng text thường (ripple mặc định chỉ bó khít theo chữ). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:gravity="center_vertical">

    <View
        android:layout_width="1dp"
        android:layout_height="24dp"
        android:layout_marginEnd="2dp"
        android:background="?android:attr/listDivider" />

    <TextView
        android:id="@+id/action_confirm_text"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_marginStart="1dp"
        android:background="?attr/selectableItemBackground"
        android:paddingStart="16dp"
        android:paddingEnd="16dp"
        android:gravity="center"
        android:textColor="?attr/actionMenuTextColor"
        android:textSize="14sp"
        android:textAllCaps="false"
        android:clickable="true"
        android:focusable="true"
        android:textStyle="bold"
        android:text="@string/action_done_select" />

</LinearLayout>
